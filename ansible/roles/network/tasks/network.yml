---

- name: Install network tools
  package: name={{ item }} state=present
  with_items:
      - ethtool
      - wpa_supplicant
      - networkmanager
      - network-manager-applet
  notify:
      - rkhunter propupdate

- name: Enable network manager service
  service: name=NetworkManager state=startet enabled=yes
